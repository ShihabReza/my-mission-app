
1.QS: What is the difference between null and undefined?
Ans: undefined:
         Means a variable has been declared but has not been assigned a value.It’s also the default return value of a function that doesn’t return anything.

    null:Represents the intentional absence of any value.It’s a value you can assign to a variable to indicate “no value” or “empty.”

2.QS: What is the use of the map() function in JavaScript? How is it different from forEach()?
Ans:Transform an array into a new array by applying a function to each element.A new array of the same length.the original array

3:QS:What is the difference between == and ===?
ANS:f the types of the two values are different, JavaScript tries to convert one value to the other type before comparing.

Can sometimes lead to unexpected results.

4:QS:What is the significance of async/await in fetching API data?
ANS:When you fetch data from an API, it happens asynchronously. That means the request takes time, and the code continues running while waiting for the response.

5:QS:Explain the concept of Scope in JavaScript (Global, Function, Block).
ANS:Scope is the region of your code where a variable or function is accessible. In JavaScript, there are mainly three types of scope
Global Scope
Function Scope
Block Scope

